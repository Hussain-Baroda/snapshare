PORT=5000
MONGO_URI=mongodb+srv://Hussy:Hussy%40123@cluster0.5tlxxnn.mongodb.net/snapshare?appName=Cluster0
JWT_SECRET=snapshare_super_secret_key_123
```

**Why `.env`?** This file holds your secrets — database passwords, JWT keys, etc. It should **never** be pushed to GitHub. Create a `.gitignore` file in `backend/`:
```
node_modules
.env